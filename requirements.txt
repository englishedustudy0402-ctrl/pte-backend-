fastapi==0.111.0
uvicorn[standard]==0.29.0
supabase==2.28.3
openai>=2.0.0
python-dotenv==1.0.1
python-multipart==0.0.9
pydantic[email]>=2.9.0
slowapi>=0.1.9
httpx>=0.27.0
python-dateutil>=2.9.0.post0
razorpay>=1.4.0

# ---------------------------------------------------------------------------
# Tier 4c — reference-sentence force alignment via WhisperX (optional).
# Deliberately NOT installed by default: these pull in ~2GB of torch /
# transformer weights that bloat the free-tier Render image and slow builds.
# To activate: uncomment the block below AND set USE_PHONEME_ALIGNER=true in
# the Render dashboard env vars (see render.yaml). The acoustic-fused phoneme
# scorer (services/phoneme.py) remains the default until then.
# ---------------------------------------------------------------------------
# whisperx
# torch
# torchaudio
# transformers