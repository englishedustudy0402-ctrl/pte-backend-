fastapi==0.111.0
uvicorn[standard]==0.29.0
supabase==2.28.3
openai>=2.0.0
python-dotenv==1.0.1
python-multipart==0.0.9
pydantic[email]>=2.9.0
slowapi>=0.1.9
httpx>=0.27.0
python-dateutil>=2.9.0.post0
razorpay>=1.4.0

# ---------------------------------------------------------------------------
# Tier 4c — reference-sentence force alignment via WhisperX (Repeat Sentence /
# Read Aloud word-level marking). Now ACTIVE (enabled in render.yaml); adds
# ~2GB of torch/transformers to the image. When the model is unavailable the
# acoustic-fused phoneme scorer (services/phoneme.py) still handles scoring.
# ---------------------------------------------------------------------------
whisperx
torch
torchaudio
transformers